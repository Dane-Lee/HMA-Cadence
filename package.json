{
  "name": "hma-cadence",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "_imports_note": "Node's package-imports namespace. These are the DEFAULTS used by dev, vitest and the admin build; vite.config.js overrides them with resolve.alias for a production client build. Declared here because a bare `#name` is not resolvable without it -- vitest could not load `#seed` at all, and `#admin-routes` had the same latent hole, hidden only because no test ever imported a module that reaches it.",
  "imports": {
    "#seed": "./src/lib/data/localSeed.js",
    "#admin-routes": "./src/routes/adminRoutes.jsx"
  },
  "description": "HMA Cadence — corrective exercise compliance PWA for the Hendrickson Navarre facility",
  "scripts": {
    "dev": "node scripts/vite-variant.mjs admin",
    "build": "node scripts/vite-variant.mjs client build",
    "build:client": "node scripts/vite-variant.mjs client build",
    "build:admin": "node scripts/vite-variant.mjs admin build",
    "preview": "node scripts/vite-variant.mjs client preview",
    "preview:admin": "node scripts/vite-variant.mjs admin preview",
    "test": "vitest run"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "jsqr": "^1.4.0",
    "qrcode": "^1.5.4",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "fake-indexeddb": "^6.2.5",
    "vite": "^6.0.7",
    "vite-plugin-pwa": "^0.21.0",
    "vitest": "^2.1.9"
  }
}
